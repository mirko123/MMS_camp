#include <stdio.h>
#include <stdlib.h>
#include <string.h>



int main() /* count lines, words, chars in input */
{
    // Exercise 1-11. How would you test the word count program? What kinds of input 
	// are most likely to uncover bugs if there are any? 
    
    /* 1. Testing with valid input.
       2. Edge cases: 
            a) Only new lines '\n'
            b) Only spaces ' ' 
            c) words with dashes, words outside Ascii
            d) all words on one line
            e) each word on a new line
            f) only backspaces '\b'
            g) large size inputs 
            h) Input with commas and other symbols, Input with special characters
       3. Invalid input
            i) Input(files) without EOF
    */
    return 1;
}

